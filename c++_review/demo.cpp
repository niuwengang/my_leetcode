#include <bits/stdc++.h>
using namespace std;

int main()
{
    // vector<int>demo(10,0);
    // for_each(demo.begin(), demo.end(), [idx = 0](int &it) mutable { it = idx++; });
    std::cout << 1e3 << endl;
    return 0;
}